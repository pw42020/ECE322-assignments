#ifndef GOFISH_H
#define GOFISH_H

/* int printStatement()
function takes in whose turn it is, if chosen rank is in other users's hand, and the rank itself
a bunch of text is shown as in the example on the website
returns nothing
*/
//void printStatement(int usergo, int cardreal, char chosen);

/* void printCardChosen()
function takes in player it's printing, char of player (user is 1, computer is 2)
and chosen rank it's searching for
function prints out all cards with the same rank as chosen from player x's deck
returns nothing
*/
//void printCardChosen( struct player* target, char player, char chosen );


#endif
